内核态仅有一个全局的内核栈：
- 所有进程/线程进入内核态后都使用该共用的内核栈

## 关键资源
- `traps.S`
- `handleInterrupt()`
```c
// 1. get irqState form intStateIRQTable[(irq)]
// 2. when state == IRQSignal ->;
// 3. get cap from intStateIRQNode[(irq)]

cap_t cap;
cap = intStateIRQNode[(irq)].cap;
if (cap_get_capType(cap) == cap_notification_cap &&
cap_notification_cap_get_capNtfnCanSend(cap)) {
	sendSignal(((notification_t *)
		cap_notification_cap_get_capNtfnPtr(cap))),
		cap_notification_cap_get_capNtfnBadge(cap));
} else {
	printf("Undelivered IRQ: %d\n", (int)(irq));
}
```


## 中断注册的关键过程节点
- `sel4_client.h::seL4_IRQHandler_SetNotification(handler, notification)`
	- 给用户使用的API
- `interrupt.c::decodeIRQHandlerInvocation()`
	- sel4内核处于内核态时进行中断注册的关键函数
```c
exception_t decodeIRQHandlerInvocation(word_t invLabel, irq_t irq)

{
	switch (invLabel) {
	...
	case IRQSetIRQHandler: {
		cap_t ntfnCap;
		cte_t *slot;
		if (current_extra_caps.excaprefs[0] == NULL) {
			current_syscall_error.type = seL4_TruncatedMessage;
			return EXCEPTION_SYSCALL_ERROR;
		}
		ntfnCap = current_extra_caps.excaprefs[0]->cap;
		slot = current_extra_caps.excaprefs[0];
		if (cap_get_capType(ntfnCap) != cap_notification_cap ||
			!cap_notification_cap_get_capNtfnCanSend(ntfnCap)) {
			if (cap_get_capType(ntfnCap) != cap_notification_cap) {
				userError("IRQSetHandler: provided cap is not an 
				notification capability.");
			} else {
				userError("IRQSetHandler: caller does not have send
				rights on the endpoint.");
			}
		current_syscall_error.type = seL4_InvalidCapability;
		current_syscall_error.invalidCapNumber = 0;
		return EXCEPTION_SYSCALL_ERROR;
		}
		setThreadState(NODE_STATE(ksCurThread), ThreadState_Restart);
		invokeIRQHandler_SetIRQHandler(irq, ntfnCap, slot);
		return EXCEPTION_NONE
	}
	...
}
```
- 这里`extra_caps_t current_extra_caps`是一个全局变量，定义在`tcb.c`文件中
	- 其内部含有几个cte指针变量
	- 主要用于调用过程中暂存extra信息！！！
		- 在`lookupExtraCaps()`其会被赋值
		- 实际的cap还是存在于TCB的能力集中【一般为发起者？？？】
	- 说实话：有点丑陋！！！

- `interrupt.c::invokeIRQHandler_SetIRQHandler()`
```c
void invokeIRQHandler_SetIRQHandler(irq_t irq, cap_t cap, cte_t *slot)

{
	cte_t *irqSlot;
	irqSlot = intStateIRQNode + IRQT_TO_IDX(irq);
	/** GHOSTUPD: "(True, gs_set_assn cteDeleteOne_'proc (-1))" */
	cteDeleteOne(irqSlot);
	cteInsert(cap, slot, irqSlot);
}
```

## extra cap 传递关键调用链
`slowpath() -> handleSyscall() -> handleInvocation() -> lookupExtraCaps() -> `
1. 会先查找sender的IPCBuffer；其中预留了固定数量的extra cap ptr位置，用于存储cap entry的地址
2. `lookupExtraCaps()`中会使用cap ptr地址，从sender的cap集中找到其对应的cap；它实现了能力空间（CSpace）中从能力指针（CPtr）到具体 CTE 槽位的解析过程
```c
exception_t lookupExtraCaps(tcb_t *thread, word_t *bufferPtr, seL4_MessageInfo_t info)
{
	lookupSlot_raw_ret_t lu_ret;
	cptr_t cptr;
	word_t i, length;
	if (!bufferPtr) {
		current_extra_caps.excaprefs[0] = NULL;
		return EXCEPTION_NONE;
	}
	
	length = seL4_MessageInfo_get_extraCaps(info);
	for (i = 0; i < length; i++) {
		cptr = getExtraCPtr(bufferPtr, i);
		lu_ret = lookupSlot(thread, cptr);
		if (lu_ret.status != EXCEPTION_NONE) {
			current_fault = seL4_Fault_CapFault_new(cptr, false);
			return lu_ret.status;
		}
		current_extra_caps.excaprefs[i] = lu_ret.slot;
	}
	
	if (i < seL4_MsgMaxExtraCaps) {
		current_extra_caps.excaprefs[i] = NULL;
	}
return EXCEPTION_NONE;
}
```